# Build

- Install [sbt](https://www.scala-sbt.org)
- from `webapp` directory, launch `sbt`:

~~~ bash
$ cd webapp
$ sbt
~~~

- Start the server

~~~
> server/reStart
~~~

- Browse http://localhost:8000

